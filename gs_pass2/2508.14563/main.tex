% 3DV 2026 Paper Template; see https://github.com/cvpr-org/gogs

\documentclass[10pt,twocolumn,letterpaper]{article}

%%%%%%%%% PAPER TYPE  - PLEASE UPDATE FOR FINAL VERSION
% \usepackage{cvpr}              % To produce the CAMERA-READY version
%\usepackage[review]{cvpr}      % To produce the REVIEW version
\PassOptionsToPackage{table}{xcolor} 
\usepackage{cvpr}
% \usepackage[pagenumbers]{cvpr} % To force page numbers, e.g. for an arXiv version

% Import additional packages in the preamble file, before hyperref
\input{preamble}

% It is strongly recommended to use hyperref, especially for the review version.
% hyperref with option pagebackref eases the reviewers' job.
% Please disable hyperref *only* if you encounter grave issues, 
% e.g. with the file validation for the camera-ready version.
%
% If you comment hyperref and then uncomment it, you should delete *.aux before re-running LaTeX.
% (Or just hit 'q' on the first LaTeX run, let it finish, and you should be clear).
\definecolor{cvprblue}{rgb}{0.21,0.49,0.74}
\usepackage[pagebackref,breaklinks,colorlinks,citecolor=cvprblue]{hyperref}
\usepackage{float} % 必须加载，解决figure*位置问题
\usepackage{graphicx} % 处理图片的核心包（必选！）
\usepackage{dblfloatfix} % 替代stfloats，处理双栏浮动体
\usepackage{csvsimple} % 用于读取CSV文件
\usepackage{booktabs}  % 强烈推荐，用于生成专业美观的表格线
\usepackage{array}      % 提供更多列格式控制（可选但好用）
%\usepackage[table]{xcolor}  % 关键宏包
\usepackage{mathtools}
\usepackage{amsmath}
\usepackage{relsize} % 用于调整字体大小
\usepackage[numbers, sort&compress]{natbib}
\usepackage{multirow}

%%%%%%%%% PAPER ID  - PLEASE UPDATE
\def\paperID{oIoNLgPNmX} % *** Enter the Paper ID here
\def\confName{3DV\xspace}
\def\confYear{2026\xspace}

%%%%%%%%% TITLE - PLEASE UPDATE
\title{GOGS: High-Fidelity Geometry and Relighting for Glossy Objects \\via Gaussian Surfels}

%%%%%%%%% AUTHORS - PLEASE UPDATE
\author{
  Xingyuan Yang\quad Min Wei \\ % 作者名字并列（用\quad分隔，比\and更紧凑）
  Chengdu University of Information Technology \\ % 统一机构（只显示一次）
  {\tt\small yangcyanx@gmail.com}\quad{\tt\small weimin@cuit.edu.cn} % 邮箱对应作者（用\quad分隔）
}
\begin{document}
% 关键：\twocolumn[...] 结构保持跨双栏显示
\twocolumn[{%
    \maketitle % 标题、作者、Paper ID
    \vspace{-2em} % 负值减少间距（推荐 -1.0em 到 -2.0em）
    % 跨双栏图片（优化后）
    \begin{figure}[H] % 强制当前位置
        \hsize=\textwidth % 确保figure宽度为跨双栏（整个页面宽度）
        \centering % 图片居中
        % 调整图片宽度为跨双栏宽度的80%（推荐0.7-0.9\textwidth）
        \includegraphics[width=1\textwidth]{res/highlight.png} 
        \caption{Our method achieves accurate geometry recovery and photorealistic rendering under novel illumination. Our rendering closely matches the GT with realistic specular effects, outperforming competitors suffering from material errors or blurred specular. Normal maps accurately capture inter-reflection geometry (e.g. cat plate reflections), unlike competitors that fail under specular interference.}
        \label{fig:highlight}
    \end{figure}
}]

\setlength{\abovedisplayskip}{4pt}
\setlength{\belowdisplayskip}{4pt}
\input{sec/0_abstract}    
\input{sec/1_intro}
\input{sec/2_RelatedWork}
\begin{figure*}
    \vspace{-1em} % 负值减少间距（推荐 -1.0em 到 -2.0em）
    \centering
    \includegraphics[width=1\linewidth]{res/pipeline.png}
    \caption{Overview of : Stage I (Sec.~\ref{sec:method-geo}) reconstructs geometry with 2DGS using split-sum shading, supervised by foundation model priors with geometric curvature losses. Stage II (Sec.~\ref{sec:method-ir}) performs inverse rendering on fixed geometry via Monte Carlo sampling, utilizing 2D Gaussian ray tracing for visibility and indirect illumination and spherical mipmap-based compensation for high-frequency specular details. The pipeline's decoupled geometry-material optimization enables high-fidelity relighting.}
    \label{fig:pipline}
    \vspace{-1em} % 负值减少间距（推荐 -1.0em 到 -2.0em）
\end{figure*}
\input{sec/3_Preliminary}
\input{sec/4_Method}
\input{sec/5_Exp}
\input{sec/6_Conclusion}
{
    \vspace{-2em}
    \bibliographystyle{unsrt}
    \bibliography{main}
}
% 将 section 的编号格式改为大写字母
\appendix
\renewcommand{\thesection}{\Alph{section}}
\clearpage
\input{sec/X_suppl}
\end{document}