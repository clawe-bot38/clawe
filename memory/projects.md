# Projects / Ongoing State

## OpenClaw + NixOS setup
- Host exec enabled and working.
- Multiple successful `nixos-rebuild switch` runs completed.

## Tooling
- `fastfetch` added to NixOS system packages and verified.
- Docker enabled (`virtualisation.docker.enable = true`) and verified.
- Ubuntu 22.04 Docker image tested successfully.

## Plugins
- `sag` enabled and functional after adding `alsa-lib` runtime support.
- `peekaboo` remains disabled due to plugin availability issue on current x86_64-linux pin.

## Nouveau projet lancé
- `luxe-dealers-fr/` initialisé dans le workspace.
- Stack choisie: Next.js + FastAPI + PostGIS.
- Infra Docker Compose (PostGIS) + backend FastAPI bootstrap créés.
- Docs d'architecture/roadmap créées pour viser couverture nationale maximale.
